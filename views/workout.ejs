<% include partials/header%>

<% include partials/navigation%>

<div class='container'>
    <div class="tab">
        <button class="tablinks" onclick="openCity(event, 'Chest')" id="defaultOpen">Chest</button>
        <button class="tablinks" onclick="openCity(event, 'Back')">Back</button>
        <button class="tablinks" onclick="openCity(event, 'Legs')">Legs</button>
        <button class="tablinks" onclick="openCity(event, 'Shoulders')">Shoulders</button>
        <button class="tablinks" onclick="openCity(event, 'Triceps')">Triceps</button>
        <button class="tablinks" onclick="openCity(event, 'Biceps')">Biceps</button>
        <button class="tablinks" onclick="openCity(event, 'Abs')">Abs</button>
    </div>

    <div id="Chest" class="tabcontent">

        <div class = "row item">

            <% Exercises.forEach(function(current){ %>
                <% if(current.Muscle === 'Chest'){ %>

                <div class = "col-md-4">
                    <div class="row item">
                        <div class="thumbnail">
                            <img src="<%= current.ImagePath %>" alt="<%= current.Name %>" class="img-thumbnail img-responsive">
                            <div class="caption">
                                <p class="Title"><strong> <%= current.Name %> </strong></p>
                            </div>
                        </div>
                        <div class='add-to-log'>
                            <button class='btn btn-primary'>Add to Log</button>
                        </div>
                    </div>
                </div>
                
                <% }%>
                <% });%>

        </div>

    </div>

    <div id="Back" class="tabcontent">

    <div class="row item">


    <% Exercises.forEach(function(current){ %>
 
    <%if(current.Muscle === 'Back'){%>
        <div class = "col-md-4">
            <div class="row item">
                <div class="thumbnail">
                    <img src="<%= current.ImagePath %>" alt="<%= current.Name %>" class="img-thumbnail img-responsive">
                    <div class="caption">
                        <p class="Title"><strong> <%= current.Name %> </strong></p>
                    </div>
                </div>
                <div class='add-to-log'>
                            <button class='btn btn-primary'>Add to Log</button>
                        </div>
            </div>
        </div>
        <% } %>
    <% });%>
    </div>

    </div>

    <div id="Legs" class="tabcontent">
    <div class="row item">
    <% Exercises.forEach(function(current){ %>
    <%if(current.Muscle === 'Legs'){%>

    <div class = "col-md-4">
        <div class="row item">
            <div class="thumbnail">
                <img src="<%= current.ImagePath %>" alt="<%= current.Name %>" class="img-thumbnail img-responsive">
                <div class="caption">
                    <p class="Title"><strong> <%= current.Name %> </strong></p>
                </div>
            </div>
            <div class='add-to-log'>
                            <button class='btn btn-primary'>Add to Log</button>
                        </div>
        </div>
    </div>
    <%}%>
<% });%>
    </div>
    </div>

    <div id="Shoulders" class="tabcontent">

    <div class="row item">
    <% Exercises.forEach(function(current){ %>
    <%if(current.Muscle === 'Shoulder'){%>
    
    <div class = "col-md-4">
        <div class="row item">
            <div class="thumbnail">
                <img src="<%= current.ImagePath %>" alt="<%= current.Name %>" class="img-thumbnail img-responsive">
                <div class="caption">
                    <p class="Title"><strong> <%= current.Name %> </strong></p>
                </div>
            </div>
            <div class='add-to-log'>
                            <button class='btn btn-primary'>Add to Log</button>
                        </div>
        </div>
    </div>
    <%}%>
    <% });%>
    </div>

    </div>

    <div id="Triceps" class="tabcontent">
        
    <div class="row item">
    <% Exercises.forEach(function(current){ %>
    <%if(current.Muscle === 'Triceps'){%>    
    <div class = "col-md-4">
        <div class="row item">
            <div class="thumbnail">
                <img src="<%= current.ImagePath %>" alt="<%= current.Name %>" class="img-thumbnail img-responsive">
                <div class="caption">
                    <p class="Title"><strong> <%= current.Name %> </strong></p>
                </div>
            </div>
            <div class='add-to-log'>
                            <button class='btn btn-primary'>Add to Log</button>
                        </div>
        </div>
    </div>
    <%}%>
<% });%>
    </div>

    </div>

    <div id="Biceps" class="tabcontent">


    <div class="row item">
    <% Exercises.forEach(function(current){ %>
    <%if(current.Muscle === 'Biceps'){%>    
    <div class = "col-md-4">
        <div class="row item">
            <div class="thumbnail">
                <img src="<%= current.ImagePath %>" alt="<%= current.Name %>" class="img-thumbnail img-responsive">
                <div class="caption">
                    <p class="Title"><strong> <%= current.Name %> </strong></p>
                </div>
            </div>
            <div class='add-to-log'>
                <button class='btn btn-primary'>Add to Log</button>
            </div>
        </div>
    </div>
    <%}%>
<% });%>
    </div>

    </div>

    <div id="Abs" class="tabcontent">

    <div class="row item">
    <% Exercises.forEach(function(current){ %>
    <%if(current.Muscle === 'Abs'){%>    
    <div class = "col-md-4">
        <div class="row item">
            <div class="thumbnail">
                <img src="<%= current.ImagePath %>" alt="<%= current.Name %>" class="img-thumbnail img-responsive">
                <div class="caption">
                    <p class="Title"><strong> <%= current.Name %> </strong></p>
                </div>
            </div>
            <div class='add-to-log'>
                            <button class='btn btn-primary'>Add to Log</button>
                        </div>
        </div>
    </div>
    <%}%>
<% });%>
    </div>

    </div>

</div>

<script src='/js/workout.js'></script>
<% include partials/footer%>